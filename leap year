#include <iostream>
using namespace std;

int main() {
  int n;

  cout << "Enter an year: ";
  cin >> n;

  if ( n % 4 == 0)
    cout << n << " is leap year.";
  else
    cout << n << " is not leap year.";

  return 0;
}
